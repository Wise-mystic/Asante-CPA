name: Build and Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci

      - name: Build app
        run: |
          export NODE_ENV=production
          npm run build || { echo "Build failed!"; exit 1; }
          echo "Build completed."

      - name: Prepare deploy branch
        run: |
          # Configure git
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # Make sure all changes are committed before switching branches
          git add .
          git commit -m "Build output before deploy" || echo "No changes to commit"
          # Fetch and checkout deploy branch, or create if missing
          if git ls-remote --heads origin deploy | grep -q deploy; then
            git fetch origin deploy:deploy
            git checkout deploy
          else
            git checkout -b deploy
          fi
          # Remove everything except .git
          find . -maxdepth 1 -not -name '.git' -not -name '.' -exec rm -rf {} +
          # Copy everything from main branch except .git, node_modules, and .github
          rsync -av --exclude='.git' --exclude='node_modules' --exclude='.github' ../ .
          git add -A
          git commit -m "Deploy: $(date)" || echo "No changes to commit"
          git push -f origin deploy

      - name: Notify deployment
        run: |
          echo "✅ Deployment completed!"
          echo "📁 Files deployed to deploy branch"
          echo "🔗 Next: Set up cPanel Git Version Control to pull from deploy branch"